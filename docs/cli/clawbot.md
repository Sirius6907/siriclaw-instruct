---
summary: "CLI reference for `SiriClaw-Instruct clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `SiriClaw-Instruct clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `SiriClaw-Instruct clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `SiriClaw-Instruct clawbot qr` (same behavior as [`SiriClaw-Instruct qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `SiriClaw-Instruct clawbot qr` -> `SiriClaw-Instruct qr`
