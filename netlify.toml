[build]
  command = "pnpm i && pnpm run ui:build"
  publish = "dist/control-ui"

[build.environment]
  NODE_VERSION = "22"
  SIRICLAW_GATEWAY_URL = "wss://api.siriclaw-instruct.monster"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
